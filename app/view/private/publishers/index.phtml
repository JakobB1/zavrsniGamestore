<div class="grid-x">
    <div class="cell large-6">
        <h1 style="font-size: 1.5rem;">Publishers</h1>
    </div>
    <div class="cell large-6" style="text-align: right">
        <a href="<?= App::config('url') ?>publishers/new">
            <i style="color:green;" title="Add new" class="fas fa-2x fa-plus-circle"></i>
        </a>
    </div>
</div>
<table>
    <thead>
        <tr>
            <th>Name</th>
            <th>Country</th>
            <th>Website</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($publishers as $publishers) : ?>
            <tr>
                <td><?= $publishers->name ?></td>
                <td><?= $publishers->country ?></td>
                <td><?= $publishers->website ?></td>
                <td>
                    <a href="<?= App::config('url') ?>publishers/change?id=<? $publishers->id ?>">
                        <i title="Change &quot;<?= $publishers->name ?>&quot;" class="fas fa-2x fa-edit"></i>
                    </a>
                    <?php if ($publishers->games === 0) : ?>
                        <a onclick="return confirm('Are you sure you want to delete <?= $publishers->name ?>?');" style="margin-left: 20px;" href="<?= App::config('url') ?>publishers/delete/<?=$publishers->id?>">
                            <i style="color: red" title="Delete <?= $publishers->name ?>" class="fas fa-2x fa-trash"></i>
                        </a>
                    <?php else : ?>
                        <i style="margin-left: 20px;color: gray" title="Cannot delete  <?= $publishers->name ?> because there are games" class="fas fa-2x fa-trash"></i>
                    <?php endif; ?>

                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>